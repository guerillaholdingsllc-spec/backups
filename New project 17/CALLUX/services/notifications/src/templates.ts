export const templates = {
  offerUnlocked: 'New {callType} call nearby. Estimated earnings: ${driverPay}. Accept before the timer expires.',
  offerLocked: 'Unlock {certName} (${price}) -> +${delta}/call.',
  safetyBanner: 'Respectful handling starts with verification. Confirm identity before transfer.',
  podOverdue: 'POD is required to release payment. Save draft if offline, then submit when connected.'
};

